new TomSelect("#prestarUsuarios",{
    create: false,
    persist: false,
    sortField: {
        field: "text",
        direction: "asc"
    }
});

new TomSelect("#prestarLibros",{
    create: false,
    persist: false,
    sortField: {
        field: "text",
        direction: "asc"
    }
});

new TomSelect("#devolverLibros",{
    create: false,
    persist: false,
    sortField: {
        field: "text",
        direction: "asc"
    }
});